services:
  - type: web
    name: apex-bank-api
    runtime: docker
    dockerContext: .
    dockerfilePath: render.Dockerfile
    plan: free
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
      - key: ASPNETCORE_URLS
        value: http://+:8080
